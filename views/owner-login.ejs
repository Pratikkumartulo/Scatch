<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scatch-OwnerLogin</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>  
    <% if(error.length>0){ %>
        <div class="absolute h-fit bg-red-300 transform translate-x-[-50%] left-[50%] top-[2%] p-1 rounded-lg" role="alert">
            <strong>warning!</strong> <%= error %>.
        </div>
    <% } %>
    <div class="w-full h-screen flex px-20">
        <div class="w-full flex items-center justify-center h-screen">
            <div class="w-full px-10">
                <h4 class="text-2xl capitalize mb-5">Admin Access</h4>
                <form action="/owner/login" method="post">
                    <input class="block bg-zinc-100 w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                        type="email" placeholder="Email" name="email">
                    <input class="block bg-zinc-100 w-full px-3 py-2 border-[1px] rounded-md mb-3 border-zinc-200"
                        type="password" placeholder="Password" name="password">
                    <input class="px-5 rounded-full py-3 mt-2 bg-blue-500 text-white" type="submit" value="Login">
                </form>
            </div>
        </div>
    </div>
</body>
</html>